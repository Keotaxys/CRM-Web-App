import { render, screen } from '@testing-library/react'; import { describe, expect, it, vi } from 'vitest'; import CustomerForm from './CustomerForm';
describe('CustomerForm image and branch policy',()=>{it('has exactly two CRM image slots and no gallery',()=>{const {container}=render(<CustomerForm onSubmit={vi.fn()}/>);expect(container.querySelectorAll('input[type="file"]')).toHaveLength(2);expect(screen.getByText('ຮູບລູກຄ້າ')).toBeInTheDocument();expect(screen.getByText('ຮູບຮ້ານ / ສະຖານທີ່')).toBeInTheDocument();});it('shows branch selection only for an Admin creating a customer',()=>{const {rerender}=render(<CustomerForm onSubmit={vi.fn()}/>);expect(screen.queryByText('ສາຂາ')).not.toBeInTheDocument();rerender(<CustomerForm onSubmit={vi.fn()} admin/>);expect(screen.getByText('ສາຂາ')).toBeInTheDocument();});});
